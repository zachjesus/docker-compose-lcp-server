Docker compose image that makes two servers one that encrypts the files the other is a Readium LCP server that is able to distribute eBooks with DRM.

The LCP server is from EDRLabs, but the script for encrypting and docker config was written by me. 

https://github.com/readium/readium-lcp-server
