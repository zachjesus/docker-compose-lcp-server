public_base_url: "http://localhost:8081"
port: 8081
dsn: "sqlite3://file::memory:?cache=shared"

access:
  username: "login"
  password: "password"

license:
  provider: "http://edrlab.org"
  profile: "http://readium.org/lcp/basic-profile"
  hint_link: "http://localhost:8081/hint/{license_id}"

status:
  renew_default_days: 7
  renew_max_days: 40
  renew_link: "http://localhost:8081/renew/{license_id}"

certificate:
  cert: "/certs/cert-edrlab-test.pem"
  private_key: "/certs/privkey-edrlab-test.pem" 